# Git Build Hook Maven Plugin
A Maven plugin that can be used to perform git related tasks during your build. By default, without any customised configuration, this plugin will simply fail the build if it is run outsite of a managed git repository.
